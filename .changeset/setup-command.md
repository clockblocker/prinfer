---
"prinfer": minor
---

Add `prinfer setup` command and improve help

- Run `prinfer setup` to manually configure MCP server and skill
- `prinfer-mcp --help` now shows setup instructions
- Better error messages when auto-install fails
